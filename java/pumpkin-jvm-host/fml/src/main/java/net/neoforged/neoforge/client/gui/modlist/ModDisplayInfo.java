package net.neoforged.neoforge.client.gui.modlist;

// How a mod presents itself in the client's mod list. A headless server never renders
// one, but mods build theirs at construction, so the type and its default base have to
// hold together well enough to be instantiated and carried around.
public interface ModDisplayInfo {
}
