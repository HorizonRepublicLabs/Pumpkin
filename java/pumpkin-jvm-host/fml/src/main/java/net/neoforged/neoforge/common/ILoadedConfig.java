package net.neoforged.neoforge.common;

/**
 * Hand-written, not generated: this interface lives in a NeoForge artifact whose sources
 * are not in the decompiled tree. On the generator's "no source found" list; do not delete
 * it as un-regenerable.
 *
 * <p>Declares nothing: it appears here only as the parameter type of the generated {@code
 * ModConfigSpec.acceptConfig}.
 */
public interface ILoadedConfig {
}
