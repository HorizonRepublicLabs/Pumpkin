package net.neoforged.fml.config;

/**
 * Hand-written, not generated: FML is published as a separate artifact whose sources are
 * not in the decompiled tree. On the generator's "no source found" list; do not delete it
 * as un-regenerable.
 *
 * <p>Declares nothing on purpose. Mods name this type when handing a spec to {@link
 * net.neoforged.fml.ModContainer#registerConfig}, and the generated {@code ModConfigSpec}
 * implements it; giving it abstract methods would oblige that generated class to declare
 * members the pruner has no reason to keep.
 */
public interface IConfigSpec {
}
