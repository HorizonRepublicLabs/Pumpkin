package net.neoforged.fml.event;

/**
 * Hand-written, not generated: FML is published as a separate artifact whose sources are
 * not in the decompiled tree. On the generator's "no source found" list; do not delete it
 * as un-regenerable.
 *
 * <p>A marker, exactly as in NeoForge: it says an event goes on the mod bus rather than
 * the game bus, and declares nothing.
 */
public interface IModBusEvent {
}
